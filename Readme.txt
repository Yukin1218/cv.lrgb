Calculate linear RGB feature as in "On line selection of discriminative tracking features" using OpenCV and show the best 5 spaces, given the image and the foreground mask. The first parameter is image and the second parameter is the foreground mask image. Note that "l49.txt" should be put in the same directory as the compiled executable file.